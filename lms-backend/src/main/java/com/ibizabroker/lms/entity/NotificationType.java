package com.ibizabroker.lms.entity;

/**
 * Notification type enum shared across persistence and DTO layers.
 */
public enum NotificationType {
    INFO,
    SUCCESS,
    WARNING,
    ERROR,
    URGENT
}
